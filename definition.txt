-if we got extension error then we need to install python latest version in system from python.org
-python is interpreted and compiled programming language its execute or run code line by line and convert 
into machine code.
-variable is a memory named location. variable defining rule: a-z, no space, use underscore between space, 
we can not start with number, no special character, no space
we cant define variable in capital
-constant varibale: we create .env file for creating constant variable ex. URL=86866876JHGJTHRDTRD, VAR=34
-any value store in variable and variable assigned in memory.
-python has reserved keywords so we cant use as a variable.
-f is a string formating 
-in dividation: we use // for avoid point or decimal value and if we want to print decimal value the use /
input function use by default provide string type output
type casting: forcrfully conversion
typ conversion: automatic convert (float has big siz which is 8 bit and integer has 4 bit)
2 string adding is a concatenate
+
GitHub Notes:
-Github use for collaboration our code.

Configuration Commands:

git --version
git config --global user.name "niteshkal03"
git config --global user.email "niteshkalangada888@gmail.com"

****for checking git configuration****
git config --list

****for initialization: Folder link with GitHub****
git init
****to check status****
git status (conditions: nothing to added, modified, new file added, deleted)
git add .
git commit -m "These are inital files"
****to switch in main branch****
git branch -M main
*** to list branch***
git branch 

git remote add origin https://github.com/niteshkal03/python_for_devops.git

****Push all files on github****
git push -u origin main

ls: show all file and folder within current folder
cd: change directory
cd .. : back folder

git 
Branch: by Default:
1. Master Branch
2. Main Branch
3.Custom Branch/ Feature Branch

****main commands run everytime****
git add .
git commit -m "message"
git push
git log (use for check to last commit information)

****control github from commandline ****
gh repo create


what is python ?
variables and its rules
arithmatic operators
Input/output
typecasting and type conversion(str, float, int)
data types:
number(int, float)
string()
boolean(true / false)

window + dot use in print() #it will give emojis
print("😊😊😊") #use window button + dot button 

Python-Assignments
01-assignments-variables
-files
02-Conditional-Statements

Strings:
The series of letters, numbers within the double inverted comma that is strings.

07th May Notes

Removing Space:
-Python By default Count Space **" "** also so 
-if we want to remove left space in input method we need to use ***lstrip()*** 
and for right ***rstrip()***

DATATYPE:

1.Numeric Datatype(int, float)
2.Strings(text)---use "", ''
3.Boolean(True, False)
4.None (Nothing) --use when no assign any value

GitHub:

Clone Repository:

-create new repo on github
-then create 1 folder on local like: github_practice
-in github_practice folder: run commands (git init, git add ., git commit -m "message", git branch -M main, git remote add origin repo-link)

*How to Clone Repo From Github to loacal:
    -git clone https://github.com/DEVENDRA-5470/Python-Assignments-109.git
*to check where code will push on github (How to check):
    -git remote -v

History Mismatch:
Diffrent file in loacal and Diffrent on github so it Mismatch.

*if github repo code and local Mismatch then first pull code from github where our local folder link with github repo.
github ke jis folder se humara github_practice folder link hai, waha (github) par agar code pehle se koi pda hua hai 
aur humne local pe jo link folder hai usme agar git clone karke kisi ka code leke aa gaye toh ---local and remote repo
 se humari history mismatch hoti hai , that is the conflict.

 Solution:
 *Pull code from remote (from Github) to Local:
    -git pull origin main --allow-unrelated-histories

    git add "folder name"
    git commit -m "updated"
    git push -u origin main
    git push -f origin main  
 
2. How to set url 
    -git remote set-url origin https://github.com/DEVENDRA-5470/Python-Assignments-109.git
    -after set url we can push our code on another person github(DEVENDRA-5470).. 
    which we set already using set-url command
    -but we got message permission denied bacause i have no permission.

so we need to collaboration:

    -first hume github pe jana hoga jis repo ko clone kiya hai usme.
    setting me jayege --> collaborator --> add people (invite using username) --> accept